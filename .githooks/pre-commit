#!/bin/sh

echo "🔍 Running pre-commit checks (make lint)..."
make lint

if [ $? -ne 0 ]; then
    echo "❌ Lint or formatting failed! Please fix the errors before committing."
    echo "💡 Tip: run 'make fmt' to auto-format your code."
    exit 1
fi

echo "✅ All checks passed!"
exit 0
